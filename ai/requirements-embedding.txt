-r ../backend/requirements.txt
sentence-transformers>=5.1,<6.0
pytest>=8.4,<9.0

# NVIDIA GPU (Windows, Python 3.13) 실험 환경에서 확인한 선택 설치:
# python -m pip install "torch==2.13.0+cu130" `
#   --index-url https://download.pytorch.org/whl/cu130 --force-reinstall
