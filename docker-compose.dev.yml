services:
  db:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"

  backend:
    environment:
      APP_ENV: development

  rag:
    ports:
      - "127.0.0.1:${RAG_PORT:-8010}:8010"

volumes:
  safemaint_postgres_data:
    name: ${POSTGRES_VOLUME_NAME:-safemaint_postgres_dev_data}
